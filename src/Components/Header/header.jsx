
import "./header.css"

const header = ({greeting}) => {
  return (
    <header
      className="headerIndex"
      style={{
        
      }}
    >
      Bienvenidos a {greeting}
    </header>
  );
};

export default header;
